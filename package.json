{
  "name": "blogview-workspace",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "test-vscode": "turbo run test-vscode",
    "dev": "turbo run dev",
    "format": "prettier -w .",
    "new-version": "changeset version && pnpm format"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "prettier": "^3.8.3",
    "turbo": "^2.9.16"
  },
  "packageManager": "pnpm@11.5.0"
}

{
  "name": "blogview-workspace",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "dev": "turbo run dev",
    "format": "prettier -w .",
    "new-version": "changeset version && pnpm format && pnpm i --ignore-scripts"
  },
  "devDependencies": {
    "@changesets/cli": "^2.19.0",
    "prettier": "^2.5.1",
    "turbo": "^1.0.26"
  },
  "turbo": {
    "baseBranch": "origin/main",
    "pipeline": {
      "build": {
        "dependsOn": [
          "^build"
        ],
        "outputs": [
          "dist/**"
        ]
      },
      "test": {},
      "dev": {
        "cache": false
      }
    }
  }
}
